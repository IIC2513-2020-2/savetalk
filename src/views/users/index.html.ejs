<a href="<%= newUserPath %>">Agregar usuario</a>

<% users.forEach((user) => { %>
  <div>
    <h2><%= user.firstName %> <%= user.lastName %></h2>
    <p><%= user.email %></p>
    <a href="<%= userPath(user.id) %>">Ver más</a>
    <a href="<%= editUserPath(user.id) %>">Editar</a>
  </div>
<% }) %>
